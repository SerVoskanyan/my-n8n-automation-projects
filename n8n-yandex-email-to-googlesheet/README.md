# n8n-yandex-email-to-googlesheet
Автоматизация n8n: Запись новых писем из Яндекс Почты в Google Таблицы

## 💡 Обзор Проекта
Этот проект демонстрирует, как использовать n8n для автоматической обработки новых входящих писем из Яндекс Почты и записи ключевой информации (Отправитель, Тема, Дата Получения, Очищенное Сообщение) в Google Таблицы. Это позволяет эффективно вести учет корреспонденции или создавать базу данных писем без ручного ввода.

## ✨ Основные Функции
- Получение новых писем из Яндекс Почты (через IMAP).
- Извлечение и структурирование данных письма: отправитель, тема, дата/время получения.
- Форматирование даты и времени в удобный для чтения формат (например, "04 июня 2025 15:30").
- Очистка тела письма от HTML-тегов, URL-адресов и лишних символов.
- Автоматическая запись обработанных данных в указанную Google Таблицу.

## 🛠️ Используемые Технологии
- **n8n**: Open-source инструмент для автоматизации workflow.
- **Яндекс Почта**: Источник входящих писем.
- **Google Sheets**: Целевая таблица для сохранения данных.
- **JavaScript**: Для кастомной обработки данных в узле "Code".

## 🚀 Как Запустить / Использовать
1. Установите и запустите n8n (локально, через Docker или облачный сервис).
2. Импортируйте Workflow:
   - Скачайте файл `Yandex_Mail_to_Google_Sheets.json`.
   - В интерфейсе n8n выберите "New Workflow" → "Import from JSON".
   - Вставьте содержимое файла или загрузите его.
3. Настройте Подключения (Credentials):
   - **Яндекс Почта (IMAP)**: В узле "Email Trigger" настройте IMAP-подключение.
   - **Google Sheets**: В узле "Google Sheets" настройте OAuth2 и укажите ID таблицы и имя листа.
4. Активируйте Workflow: Нажмите кнопку "Activate" в правом верхнем углу n8n.

## 📁 Структура Workflow
- **Email Trigger (IMAP)** — получает новые письма.
- **Set** — извлекает и переименовывает ключевые поля письма.
- **Code (JS)** — форматирует дату и очищает текст.
- **Google Sheets** — записывает обработанные данные в таблицу.

## 🔒 Важное о безопасности
В выгрузке все чувствительные данные заменены на placeholders с приставкой `REMOVE_`.  
Это сделано для безопасной публикации в GitHub.  

### Что заменено:
- Credentials (`id`, `name`) → `REMOVE_FOR_PUBLIC`
- Document ID → `REMOVE_DOCUMENT_ID_PLACEHOLDER`
- Sheet ID (`gid`) → `REMOVE_SHEET_GID_PLACEHOLDER`
- Sheet Name → `REMOVE_SHEET_NAME_PLACEHOLDER`
- Node IDs → `REMOVE_NODE_ID_PLACEHOLDER`
- Assignment IDs → `ASSIGNMENT_ID_PLACEHOLDER`
- Workflow ID → `REMOVE_NODE_ID_PLACEHOLDER`
- Instance/Version ID → `REMOVE_FOR_PUBLIC`

### Что нужно сделать:
Перед использованием воркфлоу замените все значения с приставкой `REMOVE_` на свои реальные данные.

## ✍️ Автор
[Serik](https://www.linkedin.com/in/sergey-voskanyan/)

## 📄 Лицензия
Этот проект распространяется под лицензией MIT.
